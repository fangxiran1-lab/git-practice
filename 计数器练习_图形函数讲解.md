# 计数器练习：图形函数与用法讲解

## 完整代码

```python
import tkinter as tk

num = 0

def jishuqi():
    global num
    num += 1
    label.config(text=str(num))

root = tk.Tk()                        # ① 创建主窗口
root.title("计数器")                  # ② 设置窗口标题
label = tk.Label(root, text="0", font=("Arial", 30))   # ③ 创建标签
label.pack(padx=30, pady=20)          # ④ 布局：放进窗口
btn = tk.Button(root, text="点一下", command=jishuqi)   # ⑤ 创建按钮
btn.pack(pady=10)                     # ④ 布局
root.mainloop()                       # ⑥ 启动事件循环
```

## ① tk.Tk() —— 创建主窗口

```python
root = tk.Tk()
```

- 作用：创建主窗口，它是所有控件的"总容器"。没有它什么都画不出来。
- 特点：一个程序通常只创建一个。之后所有控件都要把它作为第一个参数挂上去（tk.Label(root, ...)）。
- 对照 C：相当于 CreateWindow，但不用填任何参数。
- 常用配套：title() 设标题、geometry("400x300") 设大小、mainloop() 启动事件循环。

## ② root.title("计数器") —— 设置标题

```python
root.title("计数器")
```

设置窗口标题栏显示的文字。属于"主窗口的方法"（必须写在 tk.Tk() 创建的那个对象后面）。

## ③ tk.Label() —— 创建标签

```python
label = tk.Label(root, text="0", font=("Arial", 30))
```

| 参数 | 作用 | 例子 |
|---|---|---|
| 第一个参数 | 父容器（挂在哪） | root |
| text | 显示的文字，必须是字符串 | "0" |
| font | 字体（字体名, 字号） | ("Arial", 30) |
| fg | 文字颜色 | fg="red" |
| bg | 背景颜色 | bg="light gray" |

注意：text 只接受字符串。所以计数器里更新时要写 text=str(num)，传整数会报错。

## ④ .pack() —— 布局（放进窗口）

```python
label.pack(padx=30, pady=20)
btn.pack(pady=10)
```

- 作用：创建控件只是"造出来了"，pack() 才是把它放进窗口。不调用 .pack()，控件创建了也看不见。
- padx / pady —— 外间距（控件和窗口边缘的距离），单位像素。
- 对照 C：GUI 里没有 C 的对应物——C 得手动算坐标，pack() 是自动布局，告诉它"挂上去"就行，位置它自己排。

## ⑤ tk.Button() —— 创建按钮

```python
btn = tk.Button(root, text="点一下", command=jishuqi)
```

| 参数 | 作用 | 例子 |
|---|---|---|
| 第一个参数 | 父容器 | root |
| text | 按钮上的文字 | "点一下" |
| command | 点击时触发的函数 | jishuqi |
| width / height | 大小（按字符数算） | width=8 |
| fg / bg | 文字/背景颜色 | fg="white" |
| relief | 立体效果 | relief="raised" |

两个坑（Lesson 4 讲过，再强调一次）：

1. command=jishuqi 传的是函数名，不带括号。写成 command=jishuqi() 会在创建按钮的瞬间就执行一次，而不是点击时执行。
2. 事件驱动：root.mainloop() 之后程序不往下走，而是等用户点击。每点一次 → 自动调用 jishuqi → jishuqi 里改标签 → 界面刷新。这就是"点了有反应"的原理。

## ⑥ root.mainloop() —— 启动事件循环

```python
root.mainloop()
```

程序走到这里不会结束，窗口保持打开，不断监听用户操作。和 C 里"跑完就 return 退出"完全不同。

## 按钮触发的函数里那个 label.config(...)

```python
def jishuqi():
    global num
    num += 1
    label.config(text=str(num))
```

.config(text=...) —— 修改一个已经存在的控件的属性。创建时用 tk.Label(text="0") 设初值，之后想改就 .config(text=新值)。每次点击流程：

1. 点击按钮 → 触发 jishuqi
2. num += 1 → 数字加 1（global num 声明用它外面那个全局变量）
3. label.config(text=str(num)) → 把标签文字换成新数字

这就是事件驱动的完整闭环：创建（①②③⑤）→ 布局（④）→ 等待点击（⑥）→ 点击触发函数 → 函数改控件（config）→ 刷新显示。